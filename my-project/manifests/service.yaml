apiVersion: v1
kind: Service
metadata:
  name: my-project-service
spec:
  selector:
    app: my-project
  ports:
    - protocol: TCP
      port: 80       # The port your service exposes
      targetPort: 3000  # The port your container listens on
  type: ClusterIP
