apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-site-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: example-site-service
            port:
              number: 80
